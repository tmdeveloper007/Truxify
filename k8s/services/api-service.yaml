apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: truxify
  labels:
    app: truxify
    component: api
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
    name: http
  selector:
    app: truxify
    component: api