apiVersion: v1
kind: Service
metadata:
  name: api-service-v2
  namespace: truxify
  labels:
    app: api
    version: v2
spec:
  type: ClusterIP
  ports:
  - port: 5000
    targetPort: 5000
    protocol: TCP
    name: http
  selector:
    app: api
    version: v2
